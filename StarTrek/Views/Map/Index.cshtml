@model StarTrek.DrawModels.RectangleDraw

@{
    //ViewBag.Title = "Index";
    Layout = null;
}


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <script src="~/Scripts/raphael-min.js"></script>
</head>

<body>


    @*<canvas id="myCanvas" width="578" height="400"></canvas>*@

</body>
</html>

<script type="text/javascript">


    window.onload = function () {



        var p = Raphael(0, 0, 300, 300);

        p.rect(5, 5, 100, 100)
            .attr({ fill: "#000" })
            .click(function () {
                alert('first rectangle clicked');
            });

        p.rect(@Model.X, @Model.Y, 50, 50)
            .attr({ fill: "blue" })
            .attr("stroke", "red")
            .click(function () {
                alert('second rectangle clicked');
            });

        p.rect(75, 0, 50, 50)
            .attr({ fill: "pink" })
            .click(function () {
                alert('third rectangle clicked');
            });


        @*var canvas = document.getElementById('myCanvas'),
            context = canvas.getContext('2d');

        context.beginPath();
        context.rect(@Model.X, @Model.Y, 200, 200)
        context.click
        context.closePath();
        context.stroke();*@


        //context.beginPath();
        //context.moveTo(170, 80);
        //context.bezierCurveTo(130, 100, 130, 150, 230, 150);
        //context.bezierCurveTo(250, 180, 320, 180, 340, 150);
        //context.bezierCurveTo(420, 150, 420, 120, 390, 100);
        //context.bezierCurveTo(430, 40, 370, 30, 340, 50);
        //context.bezierCurveTo(320, 5, 250, 20, 250, 50);
        //context.bezierCurveTo(200, 5, 150, 20, 170, 80);
        //context.closePath();
        //context.stroke();


        //canvas.onclick = function (e) {
        //    alert("Hello");
        //};

        //canvas.addEventListener('click')

        //// Add event listener for `click` events.
        //canvas.addEventListener('click', function(event) {
        //    var x = event.pageX - elemLeft,
        //        y = event.pageY - elemTop;

        //    // Collision detection between clicked offset and element.
        //    elements.forEach(function(element) {
        //        if (y > element.top && y < element.top + element.height
        //            && x > element.left && x < element.left + element.width) {
        //            alert('clicked an element');
        //        }
        //    });

        //}, false);

        //// Add element.
        //elements.push({
        //    colour: '#05EFFF',
        //    width: 150,
        //    height: 100,
        //    top: 20,
        //    left: 15
        //});

        //// Render elements.
        //elements.forEach(function(element) {
        //    context.fillStyle = element.colour;
        //    context.fillRect(element.left, element.top, element.width, element.height);
        //});


    };

</script>
